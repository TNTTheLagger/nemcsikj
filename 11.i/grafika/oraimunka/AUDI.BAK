program audi;

uses graph;

var gd,gm,x,y,i:integer;

BEGIN
detectgraph(gd,gm);
initgraph(gd,gm,' ');
for i:=1 to 4 do begin
x:=(getmaxX div 5)*i;
y:=(getmaxY div 2);
circle(x,y,100);
end;
readln;
closegraph;
end.