program kkorok;

uses graph;

var gd,gm,x,y,i:integer;

BEGIN
randomize;
detectgraph(gd,gm);
initgraph(gd,gm,'c:\tp7\bgi');
for i:=1 to 10 do begin
    setcolor(random(15));
    circle(getmaxx div 2, getmaxy div 2,i*10);  {sug r=i, szorozzuk 10-el}
end;
readln;
closegraph;
END.

